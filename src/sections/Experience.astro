---
import { getLangFromUrl, useTranslations } from "@/i18n/i18n";
import { getRelativeLocaleUrl } from "astro:i18n";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<section id="experience" class="mx-auto flex h-screen w-screen max-w-6xl px-4 py-8 transition-all duration-300 lg:px-8">
  <h2 class="text-2xl font-bold">Experiencia</h2>
</section>
